# cheetah

# Normalize the reward to [0, 1] as the dm_control
# cheetah
run_speed: 10  # speed > 10, r=1; (0, 10), r=s/10, else r=0
run_backwards_speed: 8  # speed < -8, r=1; (-8, 0), r=s/8, else r=0
jump_speed: 0.5  # keep[-0.5, 0.5] r=1, else r=0
stand_speed: 0.5 # keep[-0.5, 0.5] r=1, else r=0
run_one_foot_speed: 6 # speed > 6, r=1; (0, 6), r=s/6, else r=0


























